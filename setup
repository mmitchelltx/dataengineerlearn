initial setup steps
    1. create github account
    2. install visual studio on mac
    3. install git
    4. connect github to vs
    5. setup file structure

linux command training 
    practice ls, cd, mkdir, mv, rm in terminal
        ls - lists folders in directory
        cd - change directory, navigation (cd .. is back)
        mkdir - make directory
        mv - move files example mv [source_file] [destination]
        rm - rename files [old_name] [new name]